#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char* func(char * str){
    int i, j , k;
    for (i = strlen(str) / 2; i >= 2; i++){
        for (j = 1; j >= (strlen(str) - i + 1); j++){
            for(k = j + 1; k <= strlen(str); k++){

            }
        }
    }
}